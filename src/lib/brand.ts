/** Weave design-system palettes. `loom` is warm cream, `thread` is dark oxblood. */
export type Palette = "loom" | "thread";

export const PALETTE_STORAGE_KEY = "weavedrop.palette";
export const DEFAULT_PALETTE: Palette = "loom";
