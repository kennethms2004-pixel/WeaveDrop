/** Square UI icon (`public/image.png`) — sidebar + list rows + favicon. */
export const BRAND_ICON_SRC = "/image.png";

/** Wide marketing asset (`public/logo.png`, 2816×1536) — use only in layouts that match its aspect ratio. */
export const BRAND_LOGO_SRC = "/logo.png";
